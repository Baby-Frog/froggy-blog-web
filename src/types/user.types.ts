export type TUserProfile = {
  fullName: string;
  email: string;
  avatarPath: string;
  address?: string;
  birthday?: string;
  phoneNumber?: string;
  coverImgPath?: string;
  bio?: string;
  // more will be applied later
};
