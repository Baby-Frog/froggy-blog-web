import { styled } from "styled-components";

const NewStoryMainWrapper = styled.div``;

const NewStoryMain = () => {
  return <NewStoryMainWrapper></NewStoryMainWrapper>;
};

export default NewStoryMain;
